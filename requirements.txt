# Core dependencies
python-dotenv>=0.19.0
paho-mqtt>=1.6.0
requests>=2.25.0
pydbus==0.6.0
PyGObject==3.42.2

# Note: PyGObject requires system-level dependencies:
# On Ubuntu/Debian: sudo apt-get install python3-gi python3-gi-cairo gir1.2-gtk-3.0
# On Fedora: sudo dnf install python3-gobject gtk3
# On macOS: brew install pygobject3 gtk+3